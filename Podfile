# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TripReview' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TripReview
  pod 'SnapKit', '~> 4.2.0'
  pod 'Alamofire'
  pod 'MMSegmentSlider'
  pod 'MultiSlider'
  pod 'StepSlider', '~> 1.8.0'

  target 'TripReviewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TripReviewUITests' do
    # Pods for testing
  end

end
